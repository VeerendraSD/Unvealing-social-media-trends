Developed an end-to-end data exploration and preprocessing pipeline on a synthetic social media dataset (~50K+ records, 10+ features). Cleaned data by handling missing values, removing duplicates, and treating outliers. Engineered features through binning and string processing. Performed EDA using visualizations to analyze engagement patterns and distributions. Used Python (Pandas, NumPy, Matplotlib, Seaborn) in Jupyter Notebook, gaining hands-on experience in real-world data preprocessing and ML-ready dataset preparation.
